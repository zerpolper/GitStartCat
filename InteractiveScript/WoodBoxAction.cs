using System.Collections;
using System.Collections.Generic;
using System.Net;
using UnityEngine;

public static class WoodBoxAction
{
 
    //public static void WoodBoxMoveDown(bool isMove, float smoothSpeed,)
    //{
    //    if (isMove)
    //    {
    //        float step = smoothSpeed * Time.deltaTime;
    //        transform.position = Vector3.Lerp(woodBox.transform.position, downPoint, step);
    //    }
    //}

    //public static void WoodBoxMoveUp()
    //{
    //    if (isMove)
    //    {
    //        float step = smoothSpeed * 2 * Time.deltaTime;
    //        transform.position = Vector3.Lerp(woodBox.transform.position, upPoint, step);
    //    }
    //    if (orginPoint == woodBox.transform.position)
    //    {
    //        isMove = false;
    //    }
    //}
}
